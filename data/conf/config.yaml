# Output of detailed debugging information (default: false)
# debug: true

# secret key (required, for example: dd if=/dev/urandom count=32 bs=1 | base64)
secret_key: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="

# listen addr:port (default: ":8080")
# listen: ":80"

# Log file for (default: "", mean out to stdout)
# log_access: "./data/logs/access.log"

# Log file for errors (default: "", mean out to stderr)
# log_errors: "./data/logs/errors.log"
